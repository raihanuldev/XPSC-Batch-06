#include <bits/stdc++.h>
using namespace std;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    // how much bits are on
    // cout<<__builtin_popcount(45);
    //most greatest bit
    cout<<__lg(45);
    return 0;
}